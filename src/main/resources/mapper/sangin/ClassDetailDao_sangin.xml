<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.greenlast.dao.sangin.ClassDetailDao_sangin">

    <select id="getReviewsByClassId" resultType="com.example.greenlast.dto.ClassReviewDTO">
        SELECT cr.*, f.file_url, u.name
        FROM class_review cr
        LEFT JOIN user u ON cr.user_id = u.user_id
        LEFT JOIN file f ON u.file_no = f.file_no
        WHERE cr.class_id = #{classId}
    </select>

    <!--동하형 여기 introduce-->
    <select id="getIntroduceBlockByClassId" resultType="com.example.greenlast.dto.IntroduceBlockDto">
        select * from introduce_block
        where class_id = #{classId}
    </select>
    <select id="getBlockElementByBlockId" resultType="com.example.greenlast.dto.BlockElementDto">
        select * from block_element
        where block_id = #{blockId}
    </select>
    <!--동하형 여기 introduce-->

    <select id="getClassCommunityByClassId" resultType="com.example.greenlast.dto.CommunityPostDTO">
        SELECT
        p.*,
        u.name,
        COUNT(c.comment_id) AS commentCount
        FROM community_post p
        LEFT JOIN community_comment c ON p.post_id = c.post_id
        LEFT JOIN user u ON p.user_id = u.user_id
        WHERE p.class_id = #{class_id}
        GROUP BY p.post_id
    </select>
    <select id="getCommunityPostByPostId" resultType="com.example.greenlast.dto.CommunityPostDTO">
        select * from community_post where post_id = #{postId}
    </select>
    <select id="getCommunityCommentByPostId">
        select * from community_comment where post_id = #{postId}
    </select>

    <insert id="postReview" parameterType="com.example.greenlast.dto.ClassReviewDTO">
        insert into class_review (user_id, class_id, rating, content, created_at)
        values(#{review.userId},#{review.classId},#{review.rating},#{review.content},now())
    </insert>
    <update id="postReviewStatus" parameterType="com.example.greenlast.dto.ClassReviewDTO">
        UPDATE user_payment_history
        SET review_status = 'y'
        WHERE user_id = #{review.userId} AND class_id = #{review.classId}
    </update>
    <select id="getSectionByClassId" resultType="com.example.greenlast.dto.ClassSectionDTO">
        select * from class_section
        where class_id = #{classId} and
        class_permit = 'Y'
    </select>
    <select id="getLessonBySectionId" resultType="com.example.greenlast.dto.ClassLessonDTO">
        select * from class_lesson
        where section_id = #{sectionId}
        <!--and lesson_permit = 'Y'-->
    </select>


</mapper>
